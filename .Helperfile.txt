Following steps are used to push over git.
1. git add .  // used to add all the file in that directory
2. git config --global user.email "sparkiedimension123@sgmai.com"
3. git config --global user.name "sparkiedimension"
4. git commit -m "message to be displayed for this commit"
5. git push
   //it will ask for username and password 
	// username sparkiedimension
	// password Saini@123


........That's It...............The End,.........